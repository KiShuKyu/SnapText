# SnapText
SnapText is a Python-based screen capture and OCR utility built for stable, deterministic text extraction on Windows. It streamlines the workflow from pixel capture to clipboard automation through clean module boundaries and pinned dependencies.
